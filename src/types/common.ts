export type ApiErrorResponse = {
  message: string;
};
export type FitMatchUser = {
  id: string;
  username: string;
  firstName: string;
  lastName: string;
  imageUrl: string;
  isBot: boolean;
};
